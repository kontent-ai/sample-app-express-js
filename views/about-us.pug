extends layout
include ./mixins/image-tile

block content
    -var onleft = true
    each fact, index in content_item.facts.value
        -onleft = !onleft
        +image-tile(fact.title.value, fact.description.resolveHtml(), fact.image.value[0].url, onleft)